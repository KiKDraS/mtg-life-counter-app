# Directive Sync

Bound docs + skills live. Change → adapt same run.

**Mandate** (`@frontend-dev`, `@ai-engineer`, `@code-review`):
- Load linked skills via Skill tool every run.
- Docs/skills updated → apply new rules immediately, same run. No defer.
- Before Done: verify bound docs read this run. Missing → re-read.
- Violation → rework.

**Sync binding:** read `.opencode/docs/directive-sync.md` each invocation.
Fresh reads. Violation → reject `[SYNC]`.